# 重新打开状态流转限制实施计划

1. 后端先补服务测试，覆盖拒绝 `重新打开 -> 打开` 且数据与历史不变，并验证其他允许流转不受影响。
2. 后端编辑服务增加最小状态流转校验，运行聚焦测试。
3. 前端先补编辑下拉测试，覆盖重新打开时隐藏打开、普通状态仍展示打开。
4. 前端按当前问题状态计算可选状态，并同步 Mock 接口的防绕过行为。
5. 运行前后端聚焦测试、构建与差异检查。

本次直接在当前工作区实施，不创建分支、不提交，并保留所有既有未提交改动。
